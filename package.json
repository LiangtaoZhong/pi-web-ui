{
  "name": "pi-web-ui",
  "version": "2.0.0",
  "description": "React + Vite Web UI for Pi Coding Agent",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js & cd client && npm run dev",
    "dev:server": "node server.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "prod": "cd client && npm run build && cd .. && node server.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "express": "^4.21.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
